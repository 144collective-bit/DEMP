---
// Slang taken from the collage artwork itself.
const words = ['REKT', 'NGMI', 'HFSP', 'HODL', 'DEGEN', 'EXIT LIQUIDITY', 'GM', 'APE IN'];
---
<div class="ticker" aria-hidden="true">
  <div class="ticker-track">
    <div class="ticker-seq">{words.map((w) => <span>{w}</span>)}</div>
    <div class="ticker-seq">{words.map((w) => <span>{w}</span>)}</div>
  </div>
</div>
